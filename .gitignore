node_modules/
docs/ui_bakery_prompt.md
.claude/

# Local DB credentials for tools/apply-migrations.ts — never commit
.env.local

# Local DB snapshots (contain customer data) — never commit
.snapshots/
graphify-out/
.serena/
